interface AppName {
  low: string;
  up: string;
  special: string;
  allUp: string;
  //more...
}

const name: AppName = {
  low: "failean",
  up: "Failean",
  special: "fAIlean",
  allUp: "FAILEAN",
  //more after type...
};

// package.name
// index.html.head.meta.description
// index.html.head.title
// manifest.short_name
// manifest.name

export default name;
